import './Banner.css'

export const Banner = () => {
    return (
        // As tags que parecem html, neste caso são JSX e não html
        <header className="banner">
            <img src='/imagens/banner.svg' alt='Banner'/>
        </header>
    )
}